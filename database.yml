development:
  dialect: postgres
  database: gophermart
  user: marsel
  password:
  host: 127.0.0.1
  pool: 5

test:
  url: {{envOr "DATABASE_URI" "postgresql://postgres:postgres@postgres/praktikum?sslmode=disable"}}

production:
  url: {{envOr "DATABASE_URI" "postgresql://postgres:postgres@postgres/praktikum?sslmode=disable"}}